<?php ?><!doctype html>
<html class="light" lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><?= e($documentTitle) ?></title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet">
<script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
<script>
tailwind.config = {
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        "primary": "#0053db",
        "primary-dim": "#0048c1",
        "primary-container": "#dbe1ff",
        "on-primary": "#f8f7ff",
        "on-primary-container": "#0048bf",
        "secondary": "#526074",
        "secondary-container": "#d5e3fc",
        "on-secondary-container": "#455367",
        "tertiary": "#006d4a",
        "tertiary-container": "#69f6b8",
        "on-tertiary-container": "#005a3c",
        "error": "#9f403d",
        "error-container": "#fe8983",
        "on-error-container": "#752121",
        "surface": "#faf8ff",
        "surface-container-lowest": "#ffffff",
        "surface-container-low": "#f2f3ff",
        "surface-container": "#eaedff",
        "surface-container-high": "#e2e7ff",
        "surface-container-highest": "#d9e2ff",
        "outline": "#6079b7",
        "outline-variant": "#98b1f2",
        "on-surface": "#113069",
        "on-surface-variant": "#445d99",
        "inverse-surface": "#060e20"
      },
      fontFamily: {
        headline: ["Manrope", "sans-serif"],
        body: ["Inter", "sans-serif"],
        label: ["Inter", "sans-serif"]
      }
    }
  }
}
</script>
<style>
body { font-family: 'Inter', sans-serif; }
.font-headline { font-family: 'Manrope', sans-serif; }
.material-symbols-outlined { font-variation-settings: 'FILL' 0, 'wght' 450, 'GRAD' 0, 'opsz' 24; }
.grid-fade {
  background-image:
    radial-gradient(circle at top left, rgba(0,83,219,0.08), transparent 36%),
    radial-gradient(circle at bottom right, rgba(0,109,74,0.08), transparent 32%),
    linear-gradient(180deg, rgba(255,255,255,0.82), rgba(250,248,255,0.95));
}
.page-shell {
  background:
    radial-gradient(circle at top left, rgba(0,83,219,0.07), transparent 24%),
    radial-gradient(circle at bottom right, rgba(0,109,74,0.06), transparent 22%),
    linear-gradient(180deg, #faf8ff 0%, #f5f6ff 100%);
}
</style>
</head>
<body class="bg-surface text-on-surface">

<?php if ($view === 'landing'): ?>
    <div class="min-h-screen bg-surface">
        <nav class="sticky top-0 z-40 border-b border-outline-variant/10 bg-surface/90 backdrop-blur-xl">
            <div class="mx-auto flex max-w-7xl items-center justify-between px-6 py-4 lg:px-8">
                <a href="?" class="flex items-center gap-3">
                    <span class="flex h-10 w-10 items-center justify-center rounded-xl bg-primary text-on-primary shadow-[0_28px_80px_-40px_rgba(17,48,105,0.28)]">
                        <span class="material-symbols-outlined text-lg">shield</span>
                    </span>
                    <span>
                        <span class="block font-headline text-xl font-extrabold tracking-tight">CloudDB</span>
                        <span class="block text-[10px] font-bold uppercase tracking-[0.24em] text-on-surface-variant">Infrastructure</span>
                    </span>
                </a>
                <div class="flex items-center gap-3">
                    <a href="<?= e(page_url('login')) ?>" class="<?= $buttonGhost ?>">Sign in</a>
                    <a href="#plans" class="<?= $buttonPrimary ?>">Get started</a>
                </div>
            </div>
        </nav>

        <main>
            <section class="grid-fade overflow-hidden px-6 pb-24 pt-20 lg:px-8">
                <div class="mx-auto grid max-w-7xl gap-16 lg:grid-cols-[1.15fr,0.85fr] lg:items-center">
                    <div>
                        <div class="mb-6 inline-flex items-center gap-2 rounded-full border border-outline-variant/20 bg-primary-container/45 px-4 py-2">
                            <span class="h-2 w-2 rounded-full bg-tertiary"></span>
                            <span class="text-xs font-semibold uppercase tracking-[0.22em] text-on-primary-container">Hub and node orchestration</span>
                        </div>
                        <div class="max-w-3xl space-y-4">
                            <?= render_alert($message, 'message') ?>
                            <?= render_alert($error, 'error') ?>
                        </div>
                        <h1 class="mt-8 max-w-4xl font-headline text-5xl font-extrabold leading-tight tracking-tight text-on-surface sm:text-6xl lg:text-7xl">
                            Architectural control for
                            <span class="text-primary">multi-node MariaDB</span>
                            infrastructure.
                        </h1>
                        <p class="mt-6 max-w-2xl text-lg leading-8 text-on-surface-variant">
                            Provision tenant databases across managed nodes, automate backups, issue connection files, and keep access policies visible from one calm control plane.
                        </p>
                        <div class="mt-10 flex flex-wrap gap-4">
                            <a href="#plans" class="<?= $buttonPrimary ?>">Start provisioning</a>
                            <a href="<?= e(page_url('login')) ?>" class="<?= $buttonSecondary ?>">Open console</a>
                        </div>
                        <div class="mt-14 grid gap-5 sm:grid-cols-3">
                            <div class="<?= $panel ?> p-5"><p class="text-[11px] font-bold uppercase tracking-[0.22em] text-on-surface-variant">Nodes online</p><p class="mt-3 font-headline text-4xl font-extrabold"><?= count($servers) ?></p></div>
                            <div class="<?= $panel ?> p-5"><p class="text-[11px] font-bold uppercase tracking-[0.22em] text-on-surface-variant">Plans available</p><p class="mt-3 font-headline text-4xl font-extrabold"><?= count($packages) ?></p></div>
                            <div class="<?= $panel ?> p-5"><p class="text-[11px] font-bold uppercase tracking-[0.22em] text-on-surface-variant">Backups ready</p><p class="mt-3 font-headline text-4xl font-extrabold"><?= $backupStats['queued'] + $backupStats['completed'] ?></p></div>
                        </div>
                    </div>
                    <div class="<?= $panel ?> p-6 lg:p-8">
                        <div class="rounded-[1.6rem] bg-on-surface p-6 text-white shadow-[0_28px_80px_-40px_rgba(17,48,105,0.28)]">
                            <div class="flex items-center justify-between">
                                <div>
                                    <p class="text-[11px] uppercase tracking-[0.22em] text-white/60">Operations frame</p>
                                    <h2 class="mt-2 font-headline text-2xl font-extrabold">Silent Sentinel</h2>
                                </div>
                                <span class="rounded-full bg-white/10 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.18em]">Healthy</span>
                            </div>
                            <div class="mt-8 grid gap-4 sm:grid-cols-2">
                                <div class="rounded-2xl bg-white/10 p-5">
                                    <p class="text-xs font-semibold uppercase tracking-[0.18em] text-white/55">Managed assets</p>
                                    <p class="mt-3 text-3xl font-extrabold"><?= (int)($db->query("SELECT COUNT(*) FROM tenant_dbs")->fetchColumn() ?: 0) ?></p>
                                    <p class="mt-2 text-sm text-white/70">Provisioned tenant databases across your registered nodes.</p>
                                </div>
                                <div class="rounded-2xl bg-white/10 p-5">
                                    <p class="text-xs font-semibold uppercase tracking-[0.18em] text-white/55">Storage tracked</p>
                                    <p class="mt-3 text-3xl font-extrabold"><?= e(format_storage_mb($totalStorageMb)) ?></p>
                                    <p class="mt-2 text-sm text-white/70">Usage synced from node inventory and presented per tenant.</p>
                                </div>
                            </div>
                            <div class="mt-6 rounded-2xl bg-white/8 p-5">
                                <div class="flex items-center justify-between text-sm text-white/70">
                                    <span>Platform coverage</span>
                                    <span><?= $serverTotals['healthy'] ?> healthy / <?= count($servers) ?> total nodes</span>
                                </div>
                                <div class="mt-3 h-2 overflow-hidden rounded-full bg-white/10">
                                    <div class="h-full bg-primary" style="width: <?= count($servers) > 0 ? (int)round(($serverTotals['healthy'] / count($servers)) * 100) : 0 ?>%"></div>
                                </div>
                            </div>
                            <div class="mt-8 grid gap-3">
                                <div class="flex items-center justify-between rounded-2xl bg-white/10 px-4 py-3"><div class="flex items-center gap-3"><span class="material-symbols-outlined text-primary-container">dns</span><span class="text-sm font-semibold">Node-aware provisioning</span></div><span class="text-xs text-white/55">Best-node selection</span></div>
                                <div class="flex items-center justify-between rounded-2xl bg-white/10 px-4 py-3"><div class="flex items-center gap-3"><span class="material-symbols-outlined text-primary-container">backup</span><span class="text-sm font-semibold">Backup orchestration</span></div><span class="text-xs text-white/55">Node, tenant, and database scope</span></div>
                                <div class="flex items-center justify-between rounded-2xl bg-white/10 px-4 py-3"><div class="flex items-center gap-3"><span class="material-symbols-outlined text-primary-container">shield_person</span><span class="text-sm font-semibold">Access allowlists</span></div><span class="text-xs text-white/55">Per database policy sync</span></div>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
            <section class="border-y border-outline-variant/10 bg-surface-container-low px-6 py-14 lg:px-8">
                <div class="mx-auto grid max-w-7xl gap-8 md:grid-cols-4">
                    <?php foreach ([['dns', 'Multi-node provisioning', 'Route new tenant databases to the healthiest registered node.'], ['backup', 'Backup automation', 'Request full-node, tenant, or single-database backups from the hub.'], ['terminal', 'Native phpMyAdmin', 'Open the node phpMyAdmin console directly from each asset card.'], ['history', 'Operational visibility', 'Audit actions, provisioning events, and connection changes in one feed.']] as $feature): ?>
                        <div class="<?= $panel ?> p-6"><span class="material-symbols-outlined text-3xl text-primary"><?= e($feature[0]) ?></span><h3 class="mt-4 font-headline text-lg font-bold"><?= e($feature[1]) ?></h3><p class="mt-2 text-sm leading-6 text-on-surface-variant"><?= e($feature[2]) ?></p></div>
                    <?php endforeach; ?>
                </div>
            </section>
            <section id="plans" class="px-6 py-24 lg:px-8">
                <div class="mx-auto max-w-7xl">
                    <div class="mx-auto max-w-2xl text-center">
                        <h2 class="font-headline text-4xl font-extrabold tracking-tight">Service tiers</h2>
                        <p class="mt-4 text-base leading-7 text-on-surface-variant">Choose a packaged starting point. Billing stays optional when you run the platform without Paystack.</p>
                    </div>
                    <div class="mt-14 grid gap-8 lg:grid-cols-3">
                        <?php foreach ($packages as $index => $package): ?>
                            <div class="<?= $panel ?> flex flex-col p-8 <?= $index === 1 ? 'ring-2 ring-primary/10' : '' ?>">
                                <div class="flex items-start justify-between">
                                    <div><p class="text-[11px] font-bold uppercase tracking-[0.22em] text-on-surface-variant"><?= e($package['name']) ?></p><h3 class="mt-3 font-headline text-3xl font-extrabold"><?= e(format_money((float)$package['price'])) ?></h3></div>
                                    <?php if ($index === 1): ?><span class="rounded-full bg-primary-container px-3 py-1 text-[11px] font-bold uppercase tracking-[0.18em] text-on-primary-container">Recommended</span><?php endif; ?>
                                </div>
                                <ul class="mt-8 space-y-4 text-sm text-on-surface-variant">
                                    <li class="flex items-center gap-3"><span class="material-symbols-outlined text-tertiary">check_circle</span><?= (int)$package['db_limit'] ?> database<?= (int)$package['db_limit'] === 1 ? '' : 's' ?></li>
                                    <li class="flex items-center gap-3"><span class="material-symbols-outlined text-tertiary">check_circle</span><?= (int)$package['disk_quota_gb'] ?> GB storage quota</li>
                                    <li class="flex items-center gap-3"><span class="material-symbols-outlined text-tertiary">check_circle</span><?= (int)$package['max_conns'] ?> max connections</li>
                                    <li class="flex items-center gap-3"><span class="material-symbols-outlined text-tertiary">check_circle</span><?= (int)$package['duration_days'] ?> day subscription window</li>
                                </ul>
                                <a href="<?= e(page_url('signup', ['pkg' => (int)$package['id']])) ?>" class="<?= $buttonPrimary ?> mt-10 w-full">Create account</a>
                            </div>
                        <?php endforeach; ?>
                    </div>
                </div>
            </section>
        </main>
    </div>

<?php elseif ($view === 'login'): ?>
    <div class="grid-fade flex min-h-screen items-center justify-center px-6 py-16">
        <div class="<?= $panel ?> w-full max-w-md p-8 sm:p-10">
            <div class="mb-8 flex items-center gap-3">
                <span class="flex h-12 w-12 items-center justify-center rounded-2xl bg-primary text-on-primary"><span class="material-symbols-outlined">lock</span></span>
                <div><h1 class="font-headline text-2xl font-extrabold tracking-tight">Sign in</h1><p class="text-sm text-on-surface-variant">Use the admin username or a tenant email address.</p></div>
            </div>
            <div class="space-y-3"><?= render_alert($message, 'message') ?><?= render_alert($error, 'error') ?></div>
            <form method="post" class="mt-8 space-y-5">
                <input type="hidden" name="action" value="login">
                <input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>">
                <div><label class="mb-2 block text-sm font-bold text-on-surface">Identity</label><input class="<?= $input ?>" type="text" name="email" placeholder="Admin username or tenant email" required autofocus></div>
                <div><label class="mb-2 block text-sm font-bold text-on-surface">Password</label><input class="<?= $input ?>" type="password" name="password" placeholder="Enter your password" required></div>
                <button class="<?= $buttonPrimary ?> w-full">Unlock workspace</button>
            </form>
            <div class="mt-6 flex items-center justify-between text-sm"><a href="<?= e(page_url('landing')) ?>" class="text-on-surface-variant hover:text-primary">Back to landing page</a><a href="<?= e(page_url('landing')) ?>#plans" class="text-primary hover:underline">Create an account</a></div>
        </div>
    </div>

<?php elseif ($view === 'signup'): ?>
    <div class="grid-fade flex min-h-screen items-center justify-center px-6 py-16">
        <div class="grid w-full max-w-5xl gap-8 lg:grid-cols-[0.9fr,1.1fr]">
            <div class="<?= $panel ?> p-8">
                <p class="text-[11px] font-bold uppercase tracking-[0.22em] text-on-surface-variant">Selected plan</p>
                <h1 class="mt-4 font-headline text-3xl font-extrabold tracking-tight"><?= e((string)($selectedPackage['name'] ?? 'Starter')) ?></h1>
                <p class="mt-2 text-sm text-on-surface-variant">Provision isolated MariaDB capacity with node-aware balancing and downloadable connection credentials.</p>
                <div class="mt-8 space-y-4">
                    <div class="<?= $softPanel ?> p-4"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">Price</p><p class="mt-2 font-headline text-3xl font-extrabold"><?= e(format_money((float)($selectedPackage['price'] ?? 0))) ?></p></div>
                    <div class="<?= $softPanel ?> p-4"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">Included limits</p><ul class="mt-3 space-y-2 text-sm text-on-surface-variant"><li><?= (int)($selectedPackage['db_limit'] ?? 1) ?> database<?= (int)($selectedPackage['db_limit'] ?? 1) === 1 ? '' : 's' ?></li><li><?= (int)($selectedPackage['disk_quota_gb'] ?? 1) ?> GB storage</li><li><?= (int)($selectedPackage['max_conns'] ?? 10) ?> max connections</li><li><?= (int)($selectedPackage['duration_days'] ?? 30) ?> day term</li></ul></div>
                </div>
            </div>
            <div class="<?= $panel ?> p-8 sm:p-10">
                <h2 class="font-headline text-3xl font-extrabold tracking-tight">Create tenant account</h2>
                <p class="mt-2 text-sm text-on-surface-variant">You will use this identity to provision and manage your database instances.</p>
                <div class="mt-6 space-y-3"><?= render_alert($message, 'message') ?><?= render_alert($error, 'error') ?></div>
                <form method="post" class="mt-8 space-y-5">
                    <input type="hidden" name="action" value="signup">
                    <input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>">
                    <input type="hidden" name="package_id" value="<?= (int)($selectedPackage['id'] ?? 1) ?>">
                    <div><label class="mb-2 block text-sm font-bold text-on-surface">Work email</label><input class="<?= $input ?>" type="email" name="email" placeholder="name@example.com" required autofocus></div>
                    <div><label class="mb-2 block text-sm font-bold text-on-surface">Password</label><input class="<?= $input ?>" type="password" name="password" placeholder="Choose a secure password" required></div>
                    <button class="<?= $buttonPrimary ?> w-full"><?= paystack_enabled() && (float)($selectedPackage['price'] ?? 0) > 0 ? 'Continue to billing' : 'Create account' ?></button>
                </form>
                <div class="mt-6 text-sm text-on-surface-variant"><a href="<?= e(page_url('landing')) ?>" class="hover:text-primary">Back to landing page</a></div>
            </div>
        </div>
    </div>

<?php else: ?>
    <?php
    $navItems = $is_admin ? [
        ['href' => page_url('overview'), 'icon' => 'dashboard', 'label' => 'Overview', 'active' => $view === 'overview'],
        ['href' => page_url('nodes'), 'icon' => 'dns', 'label' => 'Nodes', 'active' => $view === 'nodes'],
        ['href' => page_url('tenants'), 'icon' => 'group', 'label' => 'Tenants', 'active' => $view === 'tenants'],
        ['href' => page_url('backups'), 'icon' => 'backup', 'label' => 'Backups', 'active' => $view === 'backups'],
        ['href' => page_url('plans'), 'icon' => 'payments', 'label' => 'Plans', 'active' => $view === 'plans'],
        ['href' => page_url('activity'), 'icon' => 'history', 'label' => 'Activity', 'active' => $view === 'activity'],
        ['href' => page_url('databases'), 'icon' => 'database', 'label' => 'Databases', 'active' => in_array($view, ['databases', 'database'], true)],
        ['href' => page_url('account'), 'icon' => 'person', 'label' => 'Account', 'active' => $view === 'account'],
        ['href' => page_url('settings'), 'icon' => 'settings', 'label' => 'Settings', 'active' => $view === 'settings'],
    ] : [
        ['href' => page_url('client'), 'icon' => 'dashboard', 'label' => 'Overview', 'active' => $view === 'client'],
        ['href' => page_url('backups'), 'icon' => 'backup', 'label' => 'Backups', 'active' => $view === 'backups'],
        ['href' => page_url('plans'), 'icon' => 'payments', 'label' => 'Plans', 'active' => $view === 'plans'],
        ['href' => page_url('activity'), 'icon' => 'history', 'label' => 'Activity', 'active' => $view === 'activity'],
        ['href' => page_url('account'), 'icon' => 'person', 'label' => 'Account', 'active' => $view === 'account'],
        ['href' => page_url('settings'), 'icon' => 'settings', 'label' => 'Settings', 'active' => $view === 'settings'],
    ];
    ?>
    <div class="page-shell min-h-screen">
        <aside class="fixed left-0 top-0 z-40 hidden h-full w-64 flex-col border-r border-outline-variant/10 bg-surface-container-low px-4 py-5 lg:flex">
            <a href="<?= e($is_admin ? page_url('overview') : page_url('client')) ?>" class="mb-8 flex items-center gap-3 px-2">
                <span class="flex h-10 w-10 items-center justify-center rounded-xl bg-primary text-on-primary shadow-[0_28px_80px_-40px_rgba(17,48,105,0.28)]"><span class="material-symbols-outlined">shield</span></span>
                <span><span class="block font-headline text-xl font-extrabold tracking-tight">CloudDB</span><span class="block text-[10px] font-bold uppercase tracking-[0.22em] text-on-surface-variant"><?= $is_admin ? 'Infrastructure' : 'Tenant workspace' ?></span></span>
            </a>
            <nav class="flex-1 space-y-1">
                <?php foreach ($navItems as $item): ?>
                    <a href="<?= e($item['href']) ?>" class="flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium transition <?= $item['active'] ? 'bg-surface-container-highest text-primary shadow-sm' : 'text-on-surface-variant hover:bg-surface-container-high hover:text-on-surface' ?>">
                        <span class="material-symbols-outlined text-lg"><?= e($item['icon']) ?></span>
                        <span><?= e($item['label']) ?></span>
                    </a>
                <?php endforeach; ?>
            </nav>
            <div class="mt-6 rounded-2xl bg-on-surface p-4 text-white">
                <p class="text-[11px] font-bold uppercase tracking-[0.18em] text-white/55">Session</p>
                <p class="mt-2 text-sm font-semibold"><?= $is_admin ? e(ADMIN_USER) : e((string)($client['email'] ?? 'tenant')) ?></p>
                <p class="mt-1 text-xs text-white/60"><?= $is_admin ? 'Administrative operator' : 'Tenant access' ?></p>
                <a href="?action=logout" class="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-white/80 hover:text-white"><span class="material-symbols-outlined text-lg">logout</span><span>Sign out</span></a>
            </div>
        </aside>

        <header class="sticky top-0 z-30 border-b border-outline-variant/10 bg-surface/80 backdrop-blur-xl lg:ml-64">
            <div class="flex h-16 items-center justify-between gap-4 px-6 lg:px-8">
                <div class="flex min-w-0 items-center gap-3">
                    <div class="flex h-10 w-10 items-center justify-center rounded-xl bg-surface-container text-on-surface-variant lg:hidden"><span class="material-symbols-outlined">menu</span></div>
                    <div class="hidden max-w-md flex-1 md:block">
                        <form method="get" class="relative">
                            <input type="hidden" name="view" value="<?= e($view) ?>">
                            <?php if ($view === 'activity' && $scopeFilter !== ''): ?><input type="hidden" name="scope" value="<?= e($scopeFilter) ?>"><?php endif; ?>
                            <?php if ($view === 'backups' && $scopeFilter !== ''): ?><input type="hidden" name="scope" value="<?= e($scopeFilter) ?>"><?php endif; ?>
                            <?php if ($view === 'backups' && $statusFilter !== ''): ?><input type="hidden" name="status" value="<?= e($statusFilter) ?>"><?php endif; ?>
                            <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-on-surface-variant">search</span>
                            <input class="w-full rounded-full border-none bg-surface-container-low px-10 py-2 text-sm text-on-surface placeholder:text-on-surface-variant/60 focus:ring-2 focus:ring-primary/10" type="text" name="q" value="<?= e($searchQuery) ?>" placeholder="<?= e($topSearchPlaceholder) ?>">
                        </form>
                    </div>
                </div>
                <div class="flex items-center gap-3">
                    <span class="hidden text-right md:block"><span class="block text-sm font-bold text-on-surface"><?= $is_admin ? e(ADMIN_USER) : e((string)($client['email'] ?? 'tenant')) ?></span><span class="block text-[11px] text-on-surface-variant"><?= $is_admin ? 'Operations control' : 'Plan: ' . e((string)($client['package_name'] ?? 'Tenant')) ?></span></span>
                    <span class="flex h-10 w-10 items-center justify-center rounded-full bg-primary-container text-on-primary-container font-bold"><?= e(strtoupper(substr($is_admin ? ADMIN_USER : (string)($client['email'] ?? 'T'), 0, 1))) ?></span>
                </div>
            </div>
        </header>

        <main class="px-6 pb-12 pt-8 lg:ml-64 lg:px-8">
            <div class="mx-auto max-w-[1280px] space-y-6">
                <?php if ($downloadReady): ?>
                    <div class="<?= $panel ?> flex flex-col items-start justify-between gap-4 p-6 md:flex-row md:items-center">
                        <div><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Credential file ready</p><h2 class="mt-2 font-headline text-xl font-extrabold tracking-tight">Latest database connection file is available</h2><p class="mt-2 text-sm text-on-surface-variant">Download the generated `.env` before you trigger another credential refresh.</p></div>
                        <a href="?download=1" class="<?= $buttonPrimary ?>">Download .env</a>
                    </div>
                <?php endif; ?>
                <div class="space-y-3"><?= render_alert($message, 'message') ?><?= render_alert($error, 'error') ?></div>

                <?php if ($view === 'overview' && $is_admin): ?>
                    <div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between">
                        <div><h1 class="font-headline text-3xl font-extrabold tracking-tight">Admin dashboard</h1><p class="mt-1 text-sm text-on-surface-variant">Health, tenant activity, and orchestration controls for your hub and nodes.</p></div>
                        <div class="flex flex-wrap gap-3"><a href="<?= e(page_url('nodes', ['drawer' => 'node-create'])) ?>" class="<?= $buttonSecondary ?>">Add node</a><a href="<?= e(page_url('tenants', ['drawer' => 'tenant-create'])) ?>" class="<?= $buttonSecondary ?>">Create tenant</a><a href="<?= e(page_url('backups')) ?>" class="<?= $buttonPrimary ?>">Open backups</a></div>
                    </div>
                    <div class="grid gap-5 md:grid-cols-2 xl:grid-cols-5">
                        <?php foreach ([['Active nodes', count($servers), $serverTotals['healthy'] . ' healthy'], ['Registered tenants', count($adminClients), $tenantTotals['active'] . ' active'], ['Provisioned DBs', (int)($db->query("SELECT COUNT(*) FROM tenant_dbs")->fetchColumn() ?: 0), 'Across all nodes'], ['Storage usage', format_storage_mb($totalStorageMb), 'Tracked from node inventory'], ['Recent backups', $backupStats['queued'] + $backupStats['completed'], $backupStats['queued'] . ' queued']] as $card): ?>
                            <div class="<?= $panel ?> p-6"><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant"><?= e((string)$card[0]) ?></p><p class="mt-4 font-headline text-4xl font-extrabold tracking-tight"><?= e((string)$card[1]) ?></p><p class="mt-2 text-sm text-on-surface-variant"><?= e((string)$card[2]) ?></p></div>
                        <?php endforeach; ?>
                    </div>
                    <div class="grid gap-8 xl:grid-cols-[1.15fr,0.85fr]">
                        <div class="space-y-8">
                            <section class="<?= $panel ?> p-6">
                                <div class="mb-6 flex items-center justify-between"><div><h2 class="font-headline text-xl font-extrabold tracking-tight">Node infrastructure status</h2><p class="mt-1 text-sm text-on-surface-variant">Last-known heartbeat and current tenant load per registered node.</p></div><a href="<?= e(page_url('nodes')) ?>" class="text-sm font-semibold text-primary hover:underline">View all nodes</a></div>
                                <div class="overflow-x-auto"><table class="min-w-full text-left"><thead><tr class="border-b border-outline-variant/10 text-[11px] uppercase tracking-[0.18em] text-on-surface-variant"><th class="pb-4 pr-4">Node</th><th class="pb-4 pr-4">Host</th><th class="pb-4 pr-4">Databases</th><th class="pb-4 pr-4">Last seen</th><th class="pb-4">Status</th></tr></thead><tbody class="divide-y divide-outline-variant/10"><?php if (!$servers): ?><tr><td colspan="5" class="py-6 text-sm text-on-surface-variant">No nodes linked yet.</td></tr><?php else: foreach (array_slice($servers, 0, 6) as $server): [$chipClasses, $chipLabel] = status_chip(server_health($server)); ?><tr><td class="py-4 pr-4"><div class="font-semibold"><?= e($server['name']) ?></div><div class="text-xs text-on-surface-variant"><?= e($server['public_url']) ?></div></td><td class="py-4 pr-4 text-sm text-on-surface-variant"><?= e((string)$server['host']) ?></td><td class="py-4 pr-4 text-sm font-semibold text-on-surface"><?= (int)$server['db_count'] ?></td><td class="py-4 pr-4 text-sm text-on-surface-variant"><?= e(relative_time((string)$server['last_seen'])) ?></td><td class="py-4"><span class="inline-flex rounded-full px-3 py-1 text-xs font-bold <?= e($chipClasses) ?>"><?= e($chipLabel) ?></span></td></tr><?php endforeach; endif; ?></tbody></table></div>
                            </section>
                            <section class="<?= $panel ?> p-6">
                                <div class="mb-6 flex items-center justify-between"><div><h2 class="font-headline text-xl font-extrabold tracking-tight">Infrastructure activity</h2><p class="mt-1 text-sm text-on-surface-variant">Recent changes across node registration, provisioning, backups, and credentials.</p></div><a href="<?= e(page_url('activity')) ?>" class="text-sm font-semibold text-primary hover:underline">Full audit trail</a></div>
                                <div class="space-y-4"><?php if (!$overviewActivity): ?><p class="text-sm text-on-surface-variant">No activity recorded yet.</p><?php else: foreach ($overviewActivity as $event): [$iconName, $iconText, $iconBg] = activity_meta((string)$event['event_type']); ?><div class="flex gap-4 rounded-2xl bg-surface-container-low p-4"><span class="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl <?= e($iconBg) ?> <?= e($iconText) ?>"><span class="material-symbols-outlined"><?= e($iconName) ?></span></span><div class="min-w-0 flex-1"><div class="flex items-start justify-between gap-4"><p class="text-sm font-semibold text-on-surface"><?= e(ucfirst(str_replace('.', ' ', (string)$event['event_type']))) ?></p><time class="text-xs text-on-surface-variant"><?= e(relative_time((string)$event['created_at'])) ?></time></div><p class="mt-2 text-sm leading-6 text-on-surface-variant"><?= e((string)$event['message']) ?></p></div></div><?php endforeach; endif; ?></div>
                            </section>
                        </div>
                        <div class="space-y-8">
                            <section class="overflow-hidden rounded-2xl bg-on-surface p-6 text-white"><h2 class="font-headline text-xl font-extrabold tracking-tight">Quick actions</h2><div class="mt-6 grid gap-3"><a href="<?= e(page_url('nodes', ['drawer' => 'node-create'])) ?>" class="flex items-center justify-between rounded-2xl bg-white/10 px-4 py-3 font-semibold transition hover:bg-white/15"><span class="flex items-center gap-3"><span class="material-symbols-outlined">dns</span>Add compute node</span><span class="material-symbols-outlined text-lg">arrow_forward</span></a><a href="<?= e(page_url('tenants', ['drawer' => 'tenant-create'])) ?>" class="flex items-center justify-between rounded-2xl bg-white/10 px-4 py-3 font-semibold transition hover:bg-white/15"><span class="flex items-center gap-3"><span class="material-symbols-outlined">person_add</span>Create tenant</span><span class="material-symbols-outlined text-lg">arrow_forward</span></a><a href="<?= e(page_url('plans')) ?>" class="flex items-center justify-between rounded-2xl bg-white/10 px-4 py-3 font-semibold transition hover:bg-white/15"><span class="flex items-center gap-3"><span class="material-symbols-outlined">payments</span>Manage plans</span><span class="material-symbols-outlined text-lg">arrow_forward</span></a></div></section>
                            <section class="<?= $panel ?> p-6"><div class="mb-5 flex items-center gap-2"><span class="material-symbols-outlined text-error">warning</span><h2 class="font-headline text-xl font-extrabold tracking-tight">Attention needed</h2></div><div class="space-y-4"><?php if (!$attentionItems): ?><div class="rounded-2xl bg-tertiary-container/35 p-4 text-sm text-on-tertiary-container">No urgent infrastructure issues are open.</div><?php else: foreach ($attentionItems as $item): ?><div class="rounded-2xl bg-error-container/20 p-4"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-error-container"><?= e($item['title']) ?></p><p class="mt-2 text-sm leading-6 text-on-surface"><?= e($item['body']) ?></p></div><?php endforeach; endif; ?></div></section>
                            <section class="<?= $panel ?> p-6"><h2 class="font-headline text-lg font-extrabold tracking-tight">Scheduled maintenance</h2><p class="mt-3 text-sm leading-6 text-on-surface-variant">Use the watchdog endpoint below with cron so node heartbeats and tenant expiry stay current.</p><div class="mt-5 rounded-2xl bg-surface-container-low p-4 font-mono text-xs text-primary"><?= e(app_url(['action' => 'watchdog'])) ?></div></section>
                        </div>
                    </div>

                <?php elseif ($view === 'nodes' && $is_admin): ?>
                    <div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><h1 class="font-headline text-3xl font-extrabold tracking-tight">Nodes</h1><p class="mt-1 text-sm text-on-surface-variant">Manage registered database infrastructure nodes and their public endpoints.</p></div><a href="<?= e(page_url('nodes', ['drawer' => 'node-create'])) ?>" class="<?= $buttonPrimary ?>">Add node</a></div>
                    <div class="grid gap-5 md:grid-cols-2 xl:grid-cols-4"><?php foreach ([['Total nodes', count($servers), 'text-on-surface'], ['Healthy', $serverTotals['healthy'], 'text-tertiary'], ['Warning', $serverTotals['warning'], 'text-on-surface-variant'], ['Offline', $serverTotals['offline'], 'text-error']] as $card): ?><div class="<?= $panel ?> p-6"><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant"><?= e((string)$card[0]) ?></p><p class="mt-4 font-headline text-4xl font-extrabold <?= e((string)$card[2]) ?>"><?= e((string)$card[1]) ?></p></div><?php endforeach; ?></div>
                    <section class="<?= $panel ?> overflow-hidden">
                        <div class="flex flex-wrap items-center justify-between gap-3 border-b border-outline-variant/10 px-6 py-5"><div class="flex items-center gap-2 rounded-full bg-surface-container-low p-1 text-sm"><?php foreach (['all' => 'All', 'healthy' => 'Healthy', 'warning' => 'Warning', 'offline' => 'Offline'] as $statusKey => $statusLabel): ?><a href="<?= e(page_url('nodes', $statusKey === 'all' ? [] : ['status' => $statusKey])) ?>" class="rounded-full px-4 py-1.5 font-semibold <?= (($statusFilter ?: 'all') === $statusKey) ? 'bg-surface-container-highest text-primary shadow-sm' : 'text-on-surface-variant hover:text-on-surface' ?>"><?= e($statusLabel) ?></a><?php endforeach; ?></div><p class="text-xs text-on-surface-variant">Last refreshed: <?= e(date('H:i:s')) ?></p></div>
                        <div class="overflow-x-auto"><table class="min-w-full text-left"><thead class="bg-surface-container-low/50"><tr class="text-[11px] uppercase tracking-[0.18em] text-on-surface-variant"><th class="px-6 py-4">Node name</th><th class="px-6 py-4">DB host / IP</th><th class="px-6 py-4">Public endpoint</th><th class="px-6 py-4 text-center">DBs</th><th class="px-6 py-4">Last seen</th><th class="px-6 py-4">Status</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-outline-variant/10"><?php $filteredServers = array_values(array_filter($servers, static function (array $server) use ($statusFilter): bool { return $statusFilter === '' || server_health($server) === $statusFilter; })); ?><?php if (!$filteredServers): ?><tr><td colspan="7" class="px-6 py-8 text-sm text-on-surface-variant">No nodes match the current filter.</td></tr><?php else: foreach ($filteredServers as $server): [$chipClasses, $chipLabel] = status_chip(server_health($server)); ?><tr class="hover:bg-surface-container-low/50"><td class="px-6 py-4"><div class="font-semibold text-on-surface"><?= e($server['name']) ?></div><div class="text-xs text-on-surface-variant">phpMyAdmin: /<?= e((string)($server['pma_alias'] ?: 'phpmyadmin')) ?></div></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e((string)$server['host']) ?></td><td class="px-6 py-4 text-sm text-primary"><a class="hover:underline" href="<?= e((string)$server['public_url']) ?>" target="_blank"><?= e((string)$server['public_url']) ?></a></td><td class="px-6 py-4 text-center text-sm font-semibold"><?= (int)$server['db_count'] ?></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e(relative_time((string)$server['last_seen'])) ?></td><td class="px-6 py-4"><span class="inline-flex rounded-full px-3 py-1 text-xs font-bold <?= e($chipClasses) ?>"><?= e($chipLabel) ?></span></td><td class="px-6 py-4"><div class="flex justify-end gap-2"><form method="post"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="backup_server"><input type="hidden" name="server_id" value="<?= (int)$server['id'] ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><button class="<?= $buttonGhost ?>" <?= server_health($server) === 'offline' ? 'disabled' : '' ?>>Backup</button></form><a href="<?= e(page_url('nodes', ['drawer' => 'node-edit', 'drawer_id' => (int)$server['id']])) ?>" class="<?= $buttonGhost ?>">Edit</a><form method="post" onsubmit="return confirm('Remove this node from the hub?');"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="delete_server"><input type="hidden" name="server_id" value="<?= (int)$server['id'] ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><button class="<?= $buttonDanger ?>" <?= (int)$server['db_count'] > 0 ? 'disabled' : '' ?>>Delete</button></form></div></td></tr><?php endforeach; endif; ?></tbody></table></div>
                    </section>

                <?php elseif ($view === 'tenants' && $is_admin): ?>
                    <div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><h1 class="font-headline text-3xl font-extrabold tracking-tight">Tenants</h1><p class="mt-1 text-sm text-on-surface-variant">Manage tenant accounts, plan assignment, storage usage, and provisioning actions.</p></div><a href="<?= e(page_url('tenants', ['drawer' => 'tenant-create'])) ?>" class="<?= $buttonPrimary ?>">Create tenant</a></div>
                    <div class="grid gap-5 md:grid-cols-2 xl:grid-cols-4"><div class="<?= $panel ?> p-6"><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Total tenants</p><p class="mt-4 font-headline text-4xl font-extrabold"><?= count($adminClients) ?></p></div><div class="<?= $panel ?> p-6"><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Plan distribution</p><div class="mt-4 flex gap-6 text-sm"><?php foreach (array_slice($packages, 0, 3) as $package): ?><div><p class="font-headline text-2xl font-extrabold"><?= $planUsage[(int)$package['id']] ?? 0 ?></p><p class="text-on-surface-variant"><?= e($package['name']) ?></p></div><?php endforeach; ?></div></div><div class="<?= $panel ?> p-6"><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Active storage</p><p class="mt-4 font-headline text-4xl font-extrabold"><?= e(format_storage_mb($totalStorageMb)) ?></p></div><div class="<?= $panel ?> p-6"><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Pending action</p><p class="mt-4 font-headline text-4xl font-extrabold text-error"><?= $tenantTotals['pending'] + $tenantTotals['expired'] ?></p></div></div>
                    <section class="<?= $panel ?> overflow-hidden">
                        <div class="flex flex-wrap items-center justify-between gap-3 border-b border-outline-variant/10 px-6 py-5"><div class="flex items-center gap-2 rounded-full bg-surface-container-low p-1 text-sm"><?php foreach (['all' => 'All', 'active' => 'Active', 'pending' => 'Pending', 'expired' => 'Expired'] as $statusKey => $statusLabel): ?><a href="<?= e(page_url('tenants', $statusKey === 'all' ? [] : ['status' => $statusKey])) ?>" class="rounded-full px-4 py-1.5 font-semibold <?= (($statusFilter ?: 'all') === $statusKey) ? 'bg-surface-container-highest text-primary shadow-sm' : 'text-on-surface-variant hover:text-on-surface' ?>"><?= e($statusLabel) ?></a><?php endforeach; ?></div></div>
                        <div class="overflow-x-auto"><table class="min-w-full text-left"><thead class="bg-surface-container-low/50"><tr class="text-[11px] uppercase tracking-[0.18em] text-on-surface-variant"><th class="px-6 py-4">Tenant email</th><th class="px-6 py-4">Plan</th><th class="px-6 py-4">Status</th><th class="px-6 py-4">Expiry date</th><th class="px-6 py-4">DB count</th><th class="px-6 py-4">Storage</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-outline-variant/10"><?php $filteredClients = array_values(array_filter($adminClients, static function (array $tenant) use ($statusFilter): bool { return $statusFilter === '' || (string)$tenant['status'] === $statusFilter; })); ?><?php if (!$filteredClients): ?><tr><td colspan="7" class="px-6 py-8 text-sm text-on-surface-variant">No tenant accounts match the current filter.</td></tr><?php else: foreach ($filteredClients as $tenant): [$chipClasses, $chipLabel] = status_chip((string)$tenant['status']); ?><tr class="hover:bg-surface-container-low/50"><td class="px-6 py-4"><div class="font-semibold"><?= e($tenant['email']) ?></div><div class="text-xs text-on-surface-variant">Tenant #<?= (int)$tenant['id'] ?></div></td><td class="px-6 py-4 text-sm text-on-surface"><?= e((string)($tenant['package_name'] ?: 'Unassigned')) ?></td><td class="px-6 py-4"><span class="inline-flex rounded-full px-3 py-1 text-xs font-bold <?= e($chipClasses) ?>"><?= e($chipLabel) ?></span></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e(format_datetime((string)$tenant['expires_at'])) ?></td><td class="px-6 py-4 text-sm font-semibold"><?= (int)$tenant['db_count'] ?></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e(format_storage_mb((float)$tenant['usage_mb'])) ?></td><td class="px-6 py-4"><div class="flex justify-end gap-2"><a href="<?= e(page_url('databases', ['client_id' => (int)$tenant['id']])) ?>" class="<?= $buttonGhost ?>">View</a><form method="post"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="admin_provision"><input type="hidden" name="client_id" value="<?= (int)$tenant['id'] ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><button class="<?= $buttonGhost ?>" <?= is_client_active($tenant) ? '' : 'disabled' ?>>Provision</button></form><form method="post"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="backup_client"><input type="hidden" name="client_id" value="<?= (int)$tenant['id'] ?>"><input type="hidden" name="return_to" value="<?= e(page_url('backups')) ?>"><button class="<?= $buttonGhost ?>" <?= (int)$tenant['db_count'] > 0 ? '' : 'disabled' ?>>Backup</button></form><a href="<?= e(page_url('tenants', ['drawer' => 'tenant-edit', 'drawer_id' => (int)$tenant['id']])) ?>" class="<?= $buttonGhost ?>">Edit</a><form method="post" onsubmit="return confirm('Delete this tenant account?');"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="delete_client"><input type="hidden" name="client_id" value="<?= (int)$tenant['id'] ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><button class="<?= $buttonDanger ?>" <?= (int)$tenant['db_count'] === 0 ? '' : 'disabled' ?>>Delete</button></form></div></td></tr><?php endforeach; endif; ?></tbody></table></div>
                    </section>

                <?php elseif ($view === 'backups'): ?>
                    <div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><h1 class="font-headline text-3xl font-extrabold tracking-tight">Backups center</h1><p class="mt-1 text-sm text-on-surface-variant"><?= $is_admin ? 'Platform-wide redundancy and recovery requests across nodes, tenants, and databases.' : 'Your recent backup activity and on-demand protection requests.' ?></p></div></div>
                    <div class="grid gap-5 md:grid-cols-3"><?php foreach ([['Queued', $backupStats['queued'], 'sync', 'text-primary'], ['Completed', $backupStats['completed'], 'check_circle', 'text-tertiary'], ['Failed today', $backupStats['failed'], 'report', 'text-error']] as $card): ?><div class="<?= $panel ?> p-6"><div class="flex items-center justify-between"><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant"><?= e((string)$card[0]) ?></p><span class="material-symbols-outlined <?= e((string)$card[3]) ?>"><?= e((string)$card[2]) ?></span></div><p class="mt-4 font-headline text-4xl font-extrabold"><?= e((string)$card[1]) ?></p></div><?php endforeach; ?></div>
                    <div class="grid gap-8 xl:grid-cols-[0.82fr,1.18fr]">
                        <section class="<?= $panel ?> p-6">
                            <h2 class="font-headline text-xl font-extrabold tracking-tight">Run manual backup</h2>
                            <p class="mt-2 text-sm leading-6 text-on-surface-variant"><?= $is_admin ? 'Trigger node, tenant, or database-level backup jobs directly from the control plane.' : 'Run a backup for one of your provisioned databases.' ?></p>
                            <div class="mt-6 space-y-5">
                                <?php if ($is_admin): ?>
                                    <form method="post" class="space-y-3 rounded-2xl bg-surface-container-low p-4"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="backup_server"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><label class="block text-sm font-bold text-on-surface">Full node backup</label><select class="<?= $input ?>" name="server_id"><?php foreach ($servers as $server): ?><option value="<?= (int)$server['id'] ?>"><?= e($server['name']) ?> (<?= e((string)$server['host']) ?>)</option><?php endforeach; ?></select><button class="<?= $buttonPrimary ?> w-full">Queue node backup</button></form>
                                    <form method="post" class="space-y-3 rounded-2xl bg-surface-container-low p-4"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="backup_client"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><label class="block text-sm font-bold text-on-surface">Tenant backup</label><select class="<?= $input ?>" name="client_id"><?php foreach ($adminClients as $tenant): ?><option value="<?= (int)$tenant['id'] ?>"><?= e($tenant['email']) ?></option><?php endforeach; ?></select><button class="<?= $buttonPrimary ?> w-full">Queue tenant backup</button></form>
                                <?php endif; ?>
                                <form method="post" class="space-y-3 rounded-2xl bg-surface-container-low p-4"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="<?= $is_admin ? 'backup_db_admin' : 'backup_db' ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><label class="block text-sm font-bold text-on-surface">Database backup</label><select class="<?= $input ?>" name="tdb_id"><?php foreach ($allDatabases as $row): ?><option value="<?= (int)$row['id'] ?>"><?= e($row['db_name']) ?><?= $is_admin ? ' · ' . e($row['client_email']) : '' ?></option><?php endforeach; ?></select><button class="<?= $buttonPrimary ?> w-full">Queue database backup</button></form>
                            </div>
                        </section>
                        <section class="<?= $panel ?> overflow-hidden"><div class="flex flex-wrap items-center justify-between gap-3 border-b border-outline-variant/10 px-6 py-5"><div class="flex flex-wrap items-center gap-2 text-sm"><?php foreach (['' => 'All types', 'node' => 'Node', 'tenant' => 'Tenant', 'database' => 'Database'] as $key => $label): ?><a href="<?= e(page_url('backups', array_filter(['scope' => $key ?: null, 'status' => $statusFilter ?: null, 'q' => $searchQuery ?: null]))) ?>" class="rounded-full px-4 py-1.5 font-semibold <?= (($scopeFilter === '' && $key === '') || $scopeFilter === $key) ? 'bg-primary-container text-on-primary-container' : 'bg-surface-container text-on-surface-variant hover:text-on-surface' ?>"><?= e($label) ?></a><?php endforeach; ?></div><div class="flex items-center gap-2 text-sm"><?php foreach (['' => 'All status', 'queued' => 'Queued', 'completed' => 'Completed', 'failed' => 'Failed'] as $key => $label): ?><a href="<?= e(page_url('backups', array_filter(['scope' => $scopeFilter ?: null, 'status' => $key ?: null, 'q' => $searchQuery ?: null]))) ?>" class="rounded-full px-4 py-1.5 font-semibold <?= (($statusFilter === '' && $key === '') || $statusFilter === $key) ? 'bg-surface-container-highest text-primary shadow-sm' : 'bg-surface-container text-on-surface-variant hover:text-on-surface' ?>"><?= e($label) ?></a><?php endforeach; ?></div></div><div class="overflow-x-auto"><table class="min-w-full text-left"><thead class="bg-surface-container-low/50"><tr class="text-[11px] uppercase tracking-[0.18em] text-on-surface-variant"><th class="px-6 py-4">Type</th><th class="px-6 py-4">Target</th><th class="px-6 py-4">Node</th><th class="px-6 py-4">Requested by</th><th class="px-6 py-4">Requested at</th><th class="px-6 py-4">Status</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-outline-variant/10"><?php if (!$backupJobs): ?><tr><td colspan="7" class="px-6 py-8 text-sm text-on-surface-variant">No backup jobs match the current filter.</td></tr><?php else: foreach ($backupJobs as $job): [$chipClasses, $chipLabel] = status_chip((string)$job['status']); ?><tr class="hover:bg-surface-container-low/50"><td class="px-6 py-4 text-sm font-semibold"><?= e(ucfirst((string)$job['scope'])) ?></td><td class="px-6 py-4"><div class="font-semibold text-on-surface"><?= e((string)$job['target_label']) ?></div><?php if (!empty($job['file_name'])): ?><div class="text-xs text-on-surface-variant"><?= e((string)$job['file_name']) ?></div><?php endif; ?></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e((string)($job['server_name'] ?: 'N/A')) ?></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e(($job['requested_by_role'] ?? 'system') . (($job['client_email'] ?? '') ? ' · ' . $job['client_email'] : '')) ?></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e(format_datetime((string)$job['created_at'])) ?></td><td class="px-6 py-4"><span class="inline-flex rounded-full px-3 py-1 text-xs font-bold <?= e($chipClasses) ?>"><?= e($chipLabel) ?></span></td><td class="px-6 py-4"><div class="flex justify-end gap-2"><?php if (!empty($job['tdb_id'])): ?><a href="<?= e(page_url('database', ['id' => (int)$job['tdb_id']])) ?>" class="<?= $buttonGhost ?>">View</a><?php elseif (!empty($job['client_id']) && $is_admin): ?><a href="<?= e(page_url('databases', ['client_id' => (int)$job['client_id']])) ?>" class="<?= $buttonGhost ?>">View</a><?php else: ?><span class="text-xs text-on-surface-variant">Recorded</span><?php endif; ?></div></td></tr><?php endforeach; endif; ?></tbody></table></div></section>
                    </div>

                <?php elseif ($view === 'plans'): ?>
                    <div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><h1 class="font-headline text-3xl font-extrabold tracking-tight">Service tiers</h1><p class="mt-1 text-sm text-on-surface-variant"><?= $is_admin ? 'Configure tenant plans, quotas, and connection limits for future signups and admin-created tenants.' : 'Review the limits included in each available plan and compare against your current subscription.' ?></p></div><?php if ($is_admin): ?><a href="<?= e(page_url('plans', ['drawer' => 'plan-create'])) ?>" class="<?= $buttonPrimary ?>">Create plan</a><?php endif; ?></div>
                    <div class="grid gap-6 xl:grid-cols-3"><?php foreach ($packages as $package): $isCurrentPlan = $client && (int)$client['package_id'] === (int)$package['id']; ?><section class="<?= $panel ?> flex flex-col p-6 <?= $isCurrentPlan ? 'ring-2 ring-primary/10' : '' ?>"><div class="flex items-start justify-between"><div><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant"><?= e($package['name']) ?></p><h2 class="mt-3 font-headline text-3xl font-extrabold"><?= e(format_money((float)$package['price'])) ?></h2></div><?php if ($isCurrentPlan): ?><span class="rounded-full bg-primary-container px-3 py-1 text-[11px] font-bold uppercase tracking-[0.18em] text-on-primary-container">Current</span><?php endif; ?></div><div class="mt-8 space-y-4 text-sm"><div class="flex items-center justify-between"><span class="text-on-surface-variant">Database limit</span><span class="font-semibold"><?= (int)$package['db_limit'] ?></span></div><div class="flex items-center justify-between"><span class="text-on-surface-variant">Storage quota</span><span class="font-semibold"><?= (int)$package['disk_quota_gb'] ?> GB</span></div><div class="flex items-center justify-between"><span class="text-on-surface-variant">Max connections</span><span class="font-semibold"><?= (int)$package['max_conns'] ?></span></div><div class="flex items-center justify-between"><span class="text-on-surface-variant">Duration</span><span class="font-semibold"><?= (int)$package['duration_days'] ?> days</span></div><div class="flex items-center justify-between"><span class="text-on-surface-variant">Assigned tenants</span><span class="font-semibold"><?= $planUsage[(int)$package['id']] ?? 0 ?></span></div></div><?php if ($is_admin): ?><div class="mt-8 flex gap-3"><a href="<?= e(page_url('plans', ['drawer' => 'plan-edit', 'drawer_id' => (int)$package['id']])) ?>" class="<?= $buttonGhost ?> flex-1">Edit</a><form method="post" class="flex-1" onsubmit="return confirm('Delete this plan?');"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="delete_package"><input type="hidden" name="package_id" value="<?= (int)$package['id'] ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><button class="<?= $buttonDanger ?> w-full" <?= ($planUsage[(int)$package['id']] ?? 0) > 0 ? 'disabled' : '' ?>>Delete</button></form></div><?php endif; ?></section><?php endforeach; ?></div>
                    <section class="<?= $panel ?> overflow-hidden"><div class="border-b border-outline-variant/10 px-6 py-5"><h2 class="font-headline text-xl font-extrabold tracking-tight"><?= $is_admin ? 'Recent plan updates' : 'Plan activity' ?></h2></div><div class="overflow-x-auto"><table class="min-w-full text-left"><thead class="bg-surface-container-low/50"><tr class="text-[11px] uppercase tracking-[0.18em] text-on-surface-variant"><th class="px-6 py-4">Time</th><th class="px-6 py-4">Event</th><th class="px-6 py-4">Message</th></tr></thead><tbody class="divide-y divide-outline-variant/10"><?php if (!$planActivity): ?><tr><td colspan="3" class="px-6 py-8 text-sm text-on-surface-variant">No plan activity recorded yet.</td></tr><?php else: foreach ($planActivity as $event): ?><tr><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e(format_datetime((string)$event['created_at'])) ?></td><td class="px-6 py-4 text-sm font-semibold"><?= e(ucfirst(str_replace('.', ' ', (string)$event['event_type']))) ?></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e((string)$event['message']) ?></td></tr><?php endforeach; endif; ?></tbody></table></div></section>

                <?php elseif ($view === 'activity'): ?>
                    <div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><h1 class="font-headline text-3xl font-extrabold tracking-tight">Audit trail</h1><p class="mt-1 text-sm text-on-surface-variant"><?= $is_admin ? 'Real-time monitoring of infrastructure operations, security events, and tenant changes.' : 'Your tenant activity across provisioning, backup, and credential actions.' ?></p></div></div>
                    <div class="grid gap-5 md:grid-cols-4"><div class="<?= $panel ?> p-5 md:col-span-2"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">Quick filters</p><div class="mt-4 flex flex-wrap gap-2"><?php foreach (['' => 'All events', 'backup' => 'Backup', 'database' => 'Database', 'node' => 'Node', 'auth' => 'Auth'] as $key => $label): ?><a href="<?= e(page_url('activity', array_filter(['scope' => $key ?: null, 'q' => $searchQuery ?: null]))) ?>" class="rounded-full px-4 py-1.5 text-sm font-semibold <?= (($scopeFilter === '' && $key === '') || $scopeFilter === $key) ? 'bg-primary text-on-primary' : 'bg-surface-container text-on-surface-variant hover:text-on-surface' ?>"><?= e($label) ?></a><?php endforeach; ?></div></div><div class="<?= $panel ?> p-5"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">Event volume</p><p class="mt-4 font-headline text-4xl font-extrabold"><?= count($activityRows) ?></p><p class="mt-2 text-sm text-on-surface-variant">Recent events in the current filter window.</p></div><div class="<?= $panel ?> p-5"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">Latest event</p><p class="mt-4 font-headline text-xl font-extrabold"><?= $activityRows ? e(relative_time((string)$activityRows[0]['created_at'])) : 'No data' ?></p><p class="mt-2 text-sm text-on-surface-variant">Most recent recorded action.</p></div></div>
                    <div class="grid gap-8 xl:grid-cols-[1.15fr,0.85fr]"><section class="<?= $panel ?> p-6"><div class="mb-6 flex items-center justify-between"><h2 class="font-headline text-xl font-extrabold tracking-tight">Activity feed</h2></div><div class="space-y-4"><?php if (!$activityRows): ?><p class="text-sm text-on-surface-variant">No activity matches the current filter.</p><?php else: foreach ($activityRows as $event): [$iconName, $iconText, $iconBg] = activity_meta((string)$event['event_type']); ?><article class="rounded-2xl bg-surface-container-low p-5"><div class="flex gap-4"><span class="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl <?= e($iconBg) ?> <?= e($iconText) ?>"><span class="material-symbols-outlined"><?= e($iconName) ?></span></span><div class="min-w-0 flex-1"><div class="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between"><div><h3 class="text-sm font-semibold text-on-surface"><?= e(ucfirst(str_replace('.', ' ', (string)$event['event_type']))) ?></h3><p class="mt-2 text-sm leading-6 text-on-surface-variant"><?= e((string)$event['message']) ?></p></div><time class="text-xs text-on-surface-variant"><?= e(relative_time((string)$event['created_at'])) ?></time></div><div class="mt-3 flex flex-wrap gap-2 text-[11px] font-semibold uppercase tracking-[0.18em] text-on-surface-variant"><span>Actor: <?= e((string)$event['actor_role']) ?></span><span>Entity: <?= e((string)$event['entity_type']) ?></span></div></div></div></article><?php endforeach; endif; ?></div></section><section class="<?= $panel ?> p-6"><h2 class="font-headline text-lg font-extrabold tracking-tight">Event velocity</h2><div class="mt-6 space-y-4"><?php $eventBuckets = []; foreach ($activityRows as $event) { $bucket = explode('.', (string)$event['event_type'])[0]; $eventBuckets[$bucket] = ($eventBuckets[$bucket] ?? 0) + 1; } if (!$eventBuckets) { $eventBuckets = ['activity' => 0]; } foreach ($eventBuckets as $bucket => $count): ?><div><div class="mb-2 flex items-center justify-between text-sm"><span class="font-semibold text-on-surface"><?= e(ucfirst($bucket)) ?></span><span class="text-on-surface-variant"><?= (int)$count ?></span></div><div class="h-2 overflow-hidden rounded-full bg-surface-container"><div class="h-full bg-primary" style="width: <?= max(8, min(100, (int)round(($count / max(1, count($activityRows))) * 100))) ?>%"></div></div></div><?php endforeach; ?></div></section></div>

                <?php elseif ($view === 'databases' && $is_admin): ?>
                    <div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><h1 class="font-headline text-3xl font-extrabold tracking-tight">Databases</h1><p class="mt-1 text-sm text-on-surface-variant">Tenant database inventory with direct access to backups, connection files, and allowlist controls.</p></div></div>
                    <section class="<?= $panel ?> overflow-hidden"><div class="flex flex-wrap items-center justify-between gap-3 border-b border-outline-variant/10 px-6 py-5"><form method="get" class="flex flex-wrap gap-3"><input type="hidden" name="view" value="databases"><select class="<?= $input ?> min-w-[220px]" name="server_id"><option value="">All nodes</option><?php foreach ($servers as $server): ?><option value="<?= (int)$server['id'] ?>" <?= $serverFilter === (int)$server['id'] ? 'selected' : '' ?>><?= e($server['name']) ?></option><?php endforeach; ?></select><select class="<?= $input ?> min-w-[260px]" name="client_id"><option value="">All tenants</option><?php foreach ($adminClients as $tenant): ?><option value="<?= (int)$tenant['id'] ?>" <?= $tenantFilter === (int)$tenant['id'] ? 'selected' : '' ?>><?= e($tenant['email']) ?></option><?php endforeach; ?></select><button class="<?= $buttonSecondary ?>">Apply filters</button></form></div><div class="overflow-x-auto"><table class="min-w-full text-left"><thead class="bg-surface-container-low/50"><tr class="text-[11px] uppercase tracking-[0.18em] text-on-surface-variant"><th class="px-6 py-4">Database</th><th class="px-6 py-4">Tenant</th><th class="px-6 py-4">Node</th><th class="px-6 py-4">Usage</th><th class="px-6 py-4">Allowlist</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-outline-variant/10"><?php if (!$allDatabases): ?><tr><td colspan="6" class="px-6 py-8 text-sm text-on-surface-variant">No databases match the selected filters.</td></tr><?php else: foreach ($allDatabases as $row): ?><tr class="hover:bg-surface-container-low/50"><td class="px-6 py-4"><div class="font-semibold"><?= e($row['db_name']) ?></div><div class="text-xs text-on-surface-variant"><?= e($row['db_user']) ?></div></td><td class="px-6 py-4 text-sm text-on-surface"><?= e($row['client_email']) ?></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e($row['server_name']) ?></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e(format_storage_mb((float)$row['last_size_mb'])) ?></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e(allowed_ips_text((string)$row['allowed_ips'])) ?></td><td class="px-6 py-4"><div class="flex justify-end gap-2"><a href="<?= e(page_url('database', ['id' => (int)$row['id']])) ?>" class="<?= $buttonGhost ?>">Details</a><form method="post"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="download_db_env"><input type="hidden" name="tdb_id" value="<?= (int)$row['id'] ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><button class="<?= $buttonGhost ?>">Download .env</button></form></div></td></tr><?php endforeach; endif; ?></tbody></table></div></section>

                <?php elseif ($view === 'database' && $databaseDetail): ?>
                    <?php $pmaUrl = rtrim((string)$databaseDetail['public_url'], '/') . '/' . trim((string)($databaseDetail['pma_alias'] ?: 'phpmyadmin'), '/'); ?>
                    <div class="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><p class="text-sm font-medium text-tertiary"><?= $is_admin ? 'Tenant database' : 'Managed instance' ?></p><h1 class="mt-1 font-headline text-3xl font-extrabold tracking-tight"><?= e($databaseDetail['db_name']) ?></h1><p class="mt-2 text-sm text-on-surface-variant"><?= $is_admin ? e($databaseDetail['client_email']) . ' on ' . e($databaseDetail['server_name']) : 'Connection and backup controls for your provisioned database.' ?></p></div><div class="flex flex-wrap gap-3"><a href="<?= e($pmaUrl) ?>" target="_blank" class="<?= $buttonSecondary ?>">Open phpMyAdmin</a><form method="post"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="download_db_env"><input type="hidden" name="tdb_id" value="<?= (int)$databaseDetail['id'] ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><button class="<?= $buttonPrimary ?>">Download .env</button></form></div></div>
                    <div class="grid gap-8 xl:grid-cols-[1.05fr,0.95fr]"><div class="space-y-8"><section class="<?= $panel ?> p-6"><h2 class="mb-6 text-sm font-bold uppercase tracking-[0.18em] text-on-surface-variant">Connection details</h2><div class="grid gap-6 md:grid-cols-2"><div class="space-y-4"><div><label class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Hostname</label><p class="mt-2 text-sm font-medium text-on-surface"><?= e(server_database_endpoint($databaseDetail)['host']) ?></p></div><div><label class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Port</label><p class="mt-2 text-sm font-medium text-on-surface"><?= (int)server_database_endpoint($databaseDetail)['port'] ?></p></div></div><div class="space-y-4"><div><label class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Username</label><p class="mt-2 text-sm font-medium text-on-surface"><?= e($databaseDetail['db_user']) ?></p></div><div><label class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Password</label><div class="mt-2 rounded-xl bg-surface-container-low px-3 py-2 font-mono text-sm text-on-surface-variant">••••••••••••••••</div></div></div></div></section><section class="<?= $panel ?> p-6"><div class="mb-6 flex items-center justify-between"><h2 class="text-sm font-bold uppercase tracking-[0.18em] text-on-surface-variant">Access allowlist</h2><form method="post"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="rotate_db_password"><input type="hidden" name="tdb_id" value="<?= (int)$databaseDetail['id'] ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><button class="<?= $buttonGhost ?>">Rotate password</button></form></div><form method="post" class="space-y-4"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="update_whitelist"><input type="hidden" name="tdb_id" value="<?= (int)$databaseDetail['id'] ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><textarea class="<?= $input ?> min-h-[120px]" name="ips" placeholder="127.0.0.1, 203.0.113.10 or %"><?= e(allowed_ips_text((string)$databaseDetail['allowed_ips'])) ?></textarea><button class="<?= $buttonPrimary ?>">Sync allowlist</button></form></section></div><div class="space-y-8"><section class="<?= $panel ?> p-6"><h2 class="font-headline text-lg font-extrabold tracking-tight">Usage and placement</h2><div class="mt-6 space-y-4 text-sm"><div class="flex items-center justify-between"><span class="text-on-surface-variant">Tenant</span><span class="font-semibold"><?= e($databaseDetail['client_email']) ?></span></div><div class="flex items-center justify-between"><span class="text-on-surface-variant">Plan</span><span class="font-semibold"><?= e((string)($databaseDetail['package_name'] ?: 'Unassigned')) ?></span></div><div class="flex items-center justify-between"><span class="text-on-surface-variant">Node</span><span class="font-semibold"><?= e($databaseDetail['server_name']) ?></span></div><div class="flex items-center justify-between"><span class="text-on-surface-variant">Usage</span><span class="font-semibold"><?= e(format_storage_mb((float)$databaseDetail['last_size_mb'])) ?></span></div></div><div class="mt-6 h-2 overflow-hidden rounded-full bg-surface-container"><div class="h-full bg-primary" style="width: <?= ratio_percent((float)$databaseDetail['last_size_mb'], max(1, (float)(($databaseDetail['disk_quota_gb'] ?? 1) * 1024))) ?>%"></div></div></section><section class="<?= $panel ?> p-6"><div class="mb-6 flex items-center justify-between"><h2 class="font-headline text-lg font-extrabold tracking-tight">Backups</h2><form method="post"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="<?= $is_admin ? 'backup_db_admin' : 'backup_db' ?>"><input type="hidden" name="tdb_id" value="<?= (int)$databaseDetail['id'] ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><button class="<?= $buttonGhost ?>">Trigger backup</button></form></div><div class="space-y-3"><?php if (!$databaseBackups): ?><p class="text-sm text-on-surface-variant">No backup jobs recorded for this database yet.</p><?php else: foreach ($databaseBackups as $job): [$chipClasses, $chipLabel] = status_chip((string)$job['status']); ?><div class="rounded-2xl bg-surface-container-low p-4"><div class="flex items-center justify-between gap-3"><div><p class="text-sm font-semibold text-on-surface"><?= e((string)($job['file_name'] ?: $job['target_label'])) ?></p><p class="mt-1 text-xs text-on-surface-variant"><?= e(format_datetime((string)$job['created_at'])) ?></p></div><span class="inline-flex rounded-full px-3 py-1 text-xs font-bold <?= e($chipClasses) ?>"><?= e($chipLabel) ?></span></div></div><?php endforeach; endif; ?></div></section></div></div>

                <?php elseif ($view === 'client' && $client_id): ?>
                    <div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><div class="flex items-center gap-2 text-sm"><span class="rounded-full bg-tertiary-container px-3 py-1 font-semibold text-on-tertiary-container">Account <?= is_client_active($client ?? []) ? 'active' : 'restricted' ?></span><span class="text-on-surface-variant">Plan: <span class="font-semibold text-on-surface"><?= e((string)($client['package_name'] ?? 'Tenant')) ?></span></span></div><h1 class="mt-2 font-headline text-4xl font-extrabold tracking-tight">Client dashboard</h1><p class="mt-1 text-sm text-on-surface-variant">Infrastructure overview for <?= e((string)($client['email'] ?? 'your tenant')) ?>.</p></div><form method="post"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="provision"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><button class="<?= $buttonPrimary ?>" <?= $client && is_client_active($client) ? '' : 'disabled' ?>>Provision instance</button></form></div>
                    <div class="grid gap-5 md:grid-cols-3"><div class="<?= $panel ?> p-6"><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Databases used</p><p class="mt-4 font-headline text-4xl font-extrabold"><?= $clientDbCount ?> / <?= (int)($client['db_limit'] ?? 0) ?></p></div><div class="<?= $panel ?> p-6"><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Storage used</p><p class="mt-4 font-headline text-4xl font-extrabold"><?= e(format_storage_mb($clientUsageMb)) ?></p></div><div class="<?= $panel ?> p-6"><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Next billing</p><p class="mt-4 font-headline text-4xl font-extrabold"><?= e($client && !empty($client['expires_at']) ? date('M j', strtotime((string)$client['expires_at'])) : 'N/A') ?></p></div></div>
                    <section id="instances" class="<?= $panel ?> overflow-hidden"><div class="border-b border-outline-variant/10 px-6 py-5"><h2 class="font-headline text-xl font-extrabold tracking-tight">Active database instances</h2></div><div class="overflow-x-auto"><table class="min-w-full text-left"><thead class="bg-surface-container-low/50"><tr class="text-[11px] uppercase tracking-[0.18em] text-on-surface-variant"><th class="px-6 py-4">Database</th><th class="px-6 py-4">Node</th><th class="px-6 py-4">Usage</th><th class="px-6 py-4">Allowlist</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-outline-variant/10"><?php if (!$clientDatabases): ?><tr><td colspan="5" class="px-6 py-8 text-sm text-on-surface-variant">No databases provisioned yet. Use “Provision instance” to create your first one.</td></tr><?php else: foreach ($clientDatabases as $row): $pmaUrl = rtrim((string)$row['public_url'], '/') . '/' . trim((string)($row['pma_alias'] ?: 'phpmyadmin'), '/'); ?><tr class="hover:bg-surface-container-low/50"><td class="px-6 py-4"><div class="font-semibold"><?= e($row['db_name']) ?></div><div class="text-xs text-on-surface-variant"><?= e($row['db_user']) ?></div></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e($row['server_name']) ?></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e(format_storage_mb((float)$row['last_size_mb'])) ?></td><td class="px-6 py-4 text-sm text-on-surface-variant"><?= e(allowed_ips_text((string)$row['allowed_ips'])) ?></td><td class="px-6 py-4"><div class="flex justify-end gap-2"><a href="<?= e(page_url('database', ['id' => (int)$row['id']])) ?>" class="<?= $buttonGhost ?>">Details</a><a href="<?= e($pmaUrl) ?>" target="_blank" class="<?= $buttonGhost ?>">phpMyAdmin</a><form method="post"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="download_db_env"><input type="hidden" name="tdb_id" value="<?= (int)$row['id'] ?>"><input type="hidden" name="return_to" value="<?= e($currentUrl) ?>"><button class="<?= $buttonGhost ?>">Download .env</button></form><form method="post"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="backup_db"><input type="hidden" name="tdb_id" value="<?= (int)$row['id'] ?>"><input type="hidden" name="return_to" value="<?= e(page_url('backups')) ?>"><button class="<?= $buttonGhost ?>">Trigger backup</button></form></div></td></tr><?php endforeach; endif; ?></tbody></table></div></section>

                <?php elseif ($view === 'account'): ?>
                    <div class="max-w-4xl space-y-6"><div><h1 class="font-headline text-3xl font-extrabold tracking-tight">Account</h1><p class="mt-1 text-sm text-on-surface-variant"><?= $is_admin ? 'Administrative identity and operational ownership for this hub.' : 'Current tenant identity, plan assignment, and account lifecycle details.' ?></p></div><section class="<?= $panel ?> p-6"><div class="grid gap-6 md:grid-cols-2"><div><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Identity</p><h2 class="mt-3 font-headline text-2xl font-extrabold"><?= $is_admin ? e(ADMIN_USER) : e((string)($client['email'] ?? 'tenant')) ?></h2><p class="mt-2 text-sm text-on-surface-variant"><?= $is_admin ? 'Primary hub operator' : 'Tenant account email' ?></p></div><div><p class="text-[11px] font-bold uppercase tracking-[0.18em] text-on-surface-variant">Status</p><h2 class="mt-3 font-headline text-2xl font-extrabold"><?= $is_admin ? 'Operational' : e((string)($client['status'] ?? 'unknown')) ?></h2><p class="mt-2 text-sm text-on-surface-variant"><?= $is_admin ? 'Hub administration is enabled.' : 'Expiry: ' . e(format_datetime((string)($client['expires_at'] ?? ''))) ?></p></div></div><?php if (!$is_admin && $client): ?><div class="mt-8 grid gap-4 md:grid-cols-3"><div class="<?= $softPanel ?> p-4"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">Plan</p><p class="mt-2 font-semibold"><?= e((string)$client['package_name']) ?></p></div><div class="<?= $softPanel ?> p-4"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">DB limit</p><p class="mt-2 font-semibold"><?= (int)$client['db_limit'] ?></p></div><div class="<?= $softPanel ?> p-4"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">Storage</p><p class="mt-2 font-semibold"><?= e(format_storage_mb($clientUsageMb)) ?> of <?= (int)$client['disk_quota_gb'] ?> GB</p></div></div><?php endif; ?></section></div>

                <?php elseif ($view === 'settings'): ?>
                    <div class="max-w-5xl space-y-6"><div><h1 class="font-headline text-3xl font-extrabold tracking-tight">Settings</h1><p class="mt-1 text-sm text-on-surface-variant"><?= $is_admin ? 'Operational configuration summary for the deployed hub and node onboarding workflow.' : 'Usage guidance for connection security, backups, and credential refresh operations.' ?></p></div><?php if ($is_admin): ?><section class="<?= $panel ?> p-6"><div class="grid gap-6 md:grid-cols-2"><div class="<?= $softPanel ?> p-4"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">Alert email</p><p class="mt-2 font-semibold"><?= e(ADMIN_EMAIL !== '' ? ADMIN_EMAIL : 'Not configured') ?></p></div><div class="<?= $softPanel ?> p-4"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">SMTP</p><p class="mt-2 font-semibold"><?= SMTP_HOST !== '' ? 'Configured' : 'Disabled' ?></p></div><div class="<?= $softPanel ?> p-4"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">Billing</p><p class="mt-2 font-semibold"><?= paystack_enabled() ? 'Paystack enabled' : 'Manual / free-tier mode' ?></p></div><div class="<?= $softPanel ?> p-4"><p class="text-xs font-bold uppercase tracking-[0.18em] text-on-surface-variant">Watchdog URL</p><p class="mt-2 break-all text-sm font-medium text-primary"><?= e(app_url(['action' => 'watchdog'])) ?></p></div></div></section><?php else: ?><section class="<?= $panel ?> p-6"><h2 class="font-headline text-lg font-extrabold tracking-tight">Access and recovery guidance</h2><ul class="mt-4 space-y-3 text-sm leading-6 text-on-surface-variant"><li>Use the database details page to keep your allowlist current before connecting from new origins.</li><li>Downloading a fresh `.env` can rotate credentials automatically when older secrets are not stored.</li><li>Backup requests are queued through the assigned node and listed in the backup center.</li></ul></section><?php endif; ?></div>
                <?php endif; ?>
            </div>
        </main>

        <?php if ($is_admin && $view === 'nodes' && ($drawer === 'node-create' || ($drawer === 'node-edit' && $editServer))): ?>
            <div class="fixed inset-0 z-50 flex justify-end bg-inverse-surface/35 backdrop-blur-sm"><a href="<?= e(page_url('nodes')) ?>" class="block flex-1"></a><div class="flex h-full w-full max-w-xl flex-col bg-surface-container-lowest shadow-[0_28px_80px_-40px_rgba(17,48,105,0.28)]"><div class="border-b border-outline-variant/10 p-8"><div class="flex items-start justify-between gap-4"><div><h2 class="font-headline text-2xl font-extrabold tracking-tight"><?= $editServer ? 'Edit node' : 'Add node' ?></h2><p class="mt-2 text-sm text-on-surface-variant">Register a new physical or virtual node to your cluster.</p></div><a href="<?= e(page_url('nodes')) ?>" class="text-on-surface-variant hover:text-on-surface"><span class="material-symbols-outlined">close</span></a></div></div><form method="post" class="flex flex-1 flex-col overflow-y-auto"><div class="space-y-5 p-8"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="<?= $editServer ? 'update_server' : 'add_server' ?>"><input type="hidden" name="return_to" value="<?= e(page_url('nodes')) ?>"><?php if ($editServer): ?><input type="hidden" name="server_id" value="<?= (int)$editServer['id'] ?>"><?php endif; ?><div><label class="mb-2 block text-sm font-bold text-on-surface">Display name</label><input class="<?= $input ?>" type="text" name="name" value="<?= e((string)($editServer['name'] ?? '')) ?>" placeholder="e.g. Primary Node" required></div><div><label class="mb-2 block text-sm font-bold text-on-surface">Database host / IP</label><input class="<?= $input ?>" type="text" name="host" value="<?= e((string)($editServer['host'] ?? '')) ?>" placeholder="e.g. 10.0.0.10 or db-node.example.com" required></div><div><label class="mb-2 block text-sm font-bold text-on-surface">Public endpoint</label><input class="<?= $input ?>" type="text" name="public_url" value="<?= e((string)($editServer['public_url'] ?? '')) ?>" placeholder="e.g. http://node.example.com" required></div><div><label class="mb-2 block text-sm font-bold text-on-surface">Agent access token</label><input class="<?= $input ?>" type="text" name="agent_key" value="<?= e((string)($editServer['agent_key'] ?? '')) ?>" placeholder="Paste the node Agent Key" required></div><div><label class="mb-2 block text-sm font-bold text-on-surface">phpMyAdmin alias</label><input class="<?= $input ?>" type="text" name="pma_alias" value="<?= e((string)($editServer['pma_alias'] ?? 'phpmyadmin')) ?>" placeholder="phpmyadmin" required></div></div><div class="mt-auto flex items-center gap-4 border-t border-outline-variant/10 bg-surface-container-low px-8 py-6"><a href="<?= e(page_url('nodes')) ?>" class="<?= $buttonGhost ?> flex-1">Cancel</a><button class="<?= $buttonPrimary ?> flex-1"><?= $editServer ? 'Save node' : 'Save and test connection' ?></button></div></form></div></div>
        <?php endif; ?>

        <?php if ($is_admin && $view === 'tenants' && ($drawer === 'tenant-create' || ($drawer === 'tenant-edit' && $editTenant))): ?>
            <div class="fixed inset-0 z-50 flex justify-end bg-inverse-surface/35 backdrop-blur-sm"><a href="<?= e(page_url('tenants')) ?>" class="block flex-1"></a><div class="flex h-full w-full max-w-xl flex-col bg-surface-container-lowest shadow-[0_28px_80px_-40px_rgba(17,48,105,0.28)]"><div class="border-b border-outline-variant/10 p-8"><div class="flex items-start justify-between gap-4"><div><h2 class="font-headline text-2xl font-extrabold tracking-tight"><?= $editTenant ? 'Edit tenant' : 'Create tenant' ?></h2><p class="mt-2 text-sm text-on-surface-variant">Assign a plan, lifecycle status, and optional starter provisioning.</p></div><a href="<?= e(page_url('tenants')) ?>" class="text-on-surface-variant hover:text-on-surface"><span class="material-symbols-outlined">close</span></a></div></div><form method="post" class="flex flex-1 flex-col overflow-y-auto"><div class="space-y-5 p-8"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="<?= $editTenant ? 'update_client' : 'create_client' ?>"><input type="hidden" name="return_to" value="<?= e(page_url('tenants')) ?>"><?php if ($editTenant): ?><input type="hidden" name="client_id" value="<?= (int)$editTenant['id'] ?>"><?php endif; ?><div><label class="mb-2 block text-sm font-bold text-on-surface">Organization email</label><input class="<?= $input ?>" type="email" name="email" value="<?= e((string)($editTenant['email'] ?? '')) ?>" placeholder="admin@company.com" required></div><div><label class="mb-2 block text-sm font-bold text-on-surface"><?= $editTenant ? 'New password (optional)' : 'Initial password' ?></label><input class="<?= $input ?>" type="password" name="password" placeholder="<?= $editTenant ? 'Leave blank to keep the current password' : 'Set a secure password' ?>" <?= $editTenant ? '' : 'required' ?>></div><div><label class="mb-2 block text-sm font-bold text-on-surface">Plan type</label><select class="<?= $input ?>" name="package_id" required><?php foreach ($packages as $package): ?><option value="<?= (int)$package['id'] ?>" <?= $editTenant && (int)$package['id'] === (int)$editTenant['package_id'] ? 'selected' : '' ?>><?= e($package['name']) ?> · <?= (int)$package['db_limit'] ?> DB</option><?php endforeach; ?></select></div><div class="grid gap-5 sm:grid-cols-2"><div><label class="mb-2 block text-sm font-bold text-on-surface">Status</label><select class="<?= $input ?>" name="status"><?php foreach (['active', 'pending', 'expired'] as $state): ?><option value="<?= e($state) ?>" <?= (($editTenant['status'] ?? 'active') === $state) ? 'selected' : '' ?>><?= e(ucfirst($state)) ?></option><?php endforeach; ?></select></div><div><label class="mb-2 block text-sm font-bold text-on-surface">Expiry override</label><input class="<?= $input ?>" type="datetime-local" name="expires_at" value="<?= e(datetime_local_value($editTenant['expires_at'] ?? null)) ?>"></div></div><?php if (!$editTenant): ?><label class="flex items-start gap-3 rounded-2xl bg-surface-container-low p-4"><input class="mt-1 h-4 w-4 rounded border-outline-variant text-primary focus:ring-primary/20" type="checkbox" name="auto_provision" value="1"><span><span class="block text-sm font-semibold text-on-surface">Auto-provision starter database</span><span class="mt-1 block text-sm text-on-surface-variant">Creates a first database immediately after the tenant is created, as long as the tenant status is active.</span></span></label><?php endif; ?></div><div class="mt-auto flex items-center gap-4 border-t border-outline-variant/10 bg-surface-container-low px-8 py-6"><a href="<?= e(page_url('tenants')) ?>" class="<?= $buttonGhost ?> flex-1">Cancel</a><button class="<?= $buttonPrimary ?> flex-1"><?= $editTenant ? 'Save tenant' : 'Confirm creation' ?></button></div></form></div></div>
        <?php endif; ?>

        <?php if ($is_admin && $view === 'plans' && ($drawer === 'plan-create' || ($drawer === 'plan-edit' && $editPlan))): ?>
            <div class="fixed inset-0 z-50 flex justify-end bg-inverse-surface/35 backdrop-blur-sm"><a href="<?= e(page_url('plans')) ?>" class="block flex-1"></a><div class="flex h-full w-full max-w-xl flex-col bg-surface-container-lowest shadow-[0_28px_80px_-40px_rgba(17,48,105,0.28)]"><div class="border-b border-outline-variant/10 p-8"><div class="flex items-start justify-between gap-4"><div><h2 class="font-headline text-2xl font-extrabold tracking-tight"><?= $editPlan ? 'Edit plan' : 'Create plan' ?></h2><p class="mt-2 text-sm text-on-surface-variant">Plan changes affect new signups and admin-created tenants immediately.</p></div><a href="<?= e(page_url('plans')) ?>" class="text-on-surface-variant hover:text-on-surface"><span class="material-symbols-outlined">close</span></a></div></div><form method="post" class="flex flex-1 flex-col overflow-y-auto"><div class="space-y-5 p-8"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><input type="hidden" name="action" value="<?= $editPlan ? 'update_package' : 'create_package' ?>"><input type="hidden" name="return_to" value="<?= e(page_url('plans')) ?>"><?php if ($editPlan): ?><input type="hidden" name="package_id" value="<?= (int)$editPlan['id'] ?>"><?php endif; ?><div><label class="mb-2 block text-sm font-bold text-on-surface">Plan name</label><input class="<?= $input ?>" type="text" name="name" value="<?= e((string)($editPlan['name'] ?? '')) ?>" placeholder="Professional" required></div><div class="grid gap-5 sm:grid-cols-2"><div><label class="mb-2 block text-sm font-bold text-on-surface">Price</label><input class="<?= $input ?>" type="number" step="0.01" min="0" name="price" value="<?= e((string)($editPlan['price'] ?? '0')) ?>" required></div><div><label class="mb-2 block text-sm font-bold text-on-surface">Duration (days)</label><input class="<?= $input ?>" type="number" min="1" name="duration_days" value="<?= e((string)($editPlan['duration_days'] ?? '30')) ?>" required></div></div><div class="grid gap-5 sm:grid-cols-3"><div><label class="mb-2 block text-sm font-bold text-on-surface">Database limit</label><input class="<?= $input ?>" type="number" min="1" name="db_limit" value="<?= e((string)($editPlan['db_limit'] ?? '1')) ?>" required></div><div><label class="mb-2 block text-sm font-bold text-on-surface">Storage quota (GB)</label><input class="<?= $input ?>" type="number" min="1" name="disk_quota_gb" value="<?= e((string)($editPlan['disk_quota_gb'] ?? '1')) ?>" required></div><div><label class="mb-2 block text-sm font-bold text-on-surface">Max connections</label><input class="<?= $input ?>" type="number" min="1" name="max_conns" value="<?= e((string)($editPlan['max_conns'] ?? '10')) ?>" required></div></div></div><div class="mt-auto flex items-center gap-4 border-t border-outline-variant/10 bg-surface-container-low px-8 py-6"><a href="<?= e(page_url('plans')) ?>" class="<?= $buttonGhost ?> flex-1">Cancel</a><button class="<?= $buttonPrimary ?> flex-1"><?= $editPlan ? 'Save plan' : 'Create plan' ?></button></div></form></div></div>
        <?php endif; ?>
    </div>
<?php endif; ?>

</body>
</html>
